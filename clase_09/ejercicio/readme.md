i# Actividades Clase Número 9

## Actividad 1: Creando un Formulario de Registro

**Consigna:**  
Crea un formulario de registro de usuario con los siguientes campos:

- Nombre completo
- Correo electrónico
- Contraseña
- Confirmar contraseña
- Un botón de envío

**Requisitos:**  
✅ Usa etiquetas `<label>` correctamente asociadas a los `<input>`.  
✅ Aplica estilos con Bulma (`.input`, `.field`, `.button`).  
✅ Usa `method="POST"` en el `<form>`.  

**💡 Extra:** Agrega un `<textarea>` para que la usuaria pueda escribir una descripción sobre sí misma.

---

## Actividad 2: Mejorando el Formulario con Íconos

**Consigna:**  
Toma el formulario de la Actividad 1 y agrégale íconos en los inputs usando Bulma.

**Requisitos:**  
✅ Usa `.has-icons-left` para agregar un icono a la izquierda de los inputs.  
✅ Usa clases de Bulma para los estilos (`.box`, `.label`).  
✅ Agrega un botón con la clase `.is-primary`.

---

## Actividad 3: Creando un Navbar Navegable

**Consigna:**  
Crea un navbar con Bulma que contenga los siguientes elementos:

- Un logo a la izquierda
- Tres enlaces de navegación: **Inicio**, **Acerca de**, **Contacto**
- Un botón de inicio de sesión a la derecha

**Requisitos:**  
✅ Usa `.navbar`, `.navbar-menu`, `.navbar-start`, `.navbar-end`.  
✅ Usa `.navbar-burger` para que el menú se adapte a móviles.  
✅ Agrega estilos con Bulma (`.is-primary` para el botón).  

**💡 Extra:** Cambia el color del navbar agregando `.is-dark`.

---

## Actividad 4: Creando una Página con Formulario y Navbar

**Consigna:**  
Arma una página web que contenga:

1. Un **navbar** con tres enlaces y un botón de login.
2. Un **formulario de contacto** con nombre, email y mensaje.
3. Un **botón de enviar** estilizado con Bulma.

**Requisitos:**  
✅ Usa el sistema de **grillas de CSS** para centrar el contenido.  
✅ Usa **Bulma** para estilizar los elementos.  
✅ Usa `.container` y `.columns` para organizar el diseño.

---

## Actividad 5: Personalizando un Botón

**Consigna:**  
Crea un botón de peligro (`is-danger`) con borde (`is-outlined`).

**Requisitos:**  
✅ Usa `.button.is-danger.is-outlined`.  
✅ Agrega un icono al botón usando `.icon`.  
✅ Coloca el botón dentro de un div con la clase `.box` para resaltarlo.
